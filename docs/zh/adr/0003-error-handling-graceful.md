# ADR-0003: 优雅降级的错误处理

## 状态
已接受

## 上下文
终端错误（帧中 resize、tty 丢失、写入失败）必须妥善处理，禁止在渲染循环中 panic（准则 §3.4 要求）。

## 决策
**优雅降级 (Graceful degradation)** — 内部捕获并处理错误：
- 帧中 Resize → 跳过当前帧，下一帧重新布局。
- 写入错误 → 通过错误通道 `chan error` 上报。
- TTY 丢失 → 清理终端状态并退出。

## 依据
- 遵守渲染循环零 panic 准则。
- 错误通道为非阻塞发送。
- 保证终端资源平滑清理。
